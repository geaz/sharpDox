/*using SharpDox.Build;
using SharpDox.Core;

namespace SharpDox.Vsix
{
    internal class SharpDoxController
    {
        private readonly BuildController _buildController;
        private readonly OutputController _outputController;

        public SharpDoxController(OutputController outputController)
        {
            var mainContainerConfig = new MainContainerConfig();
            var mainContainer = mainContainerConfig.BuildContainer();

            mainContainer.Resolve<SDConsole>().Start(args);

            _outputController = outputController;
        }

        public void BuildDocumentation(string configFilePath)
        {
            
        }
    }
}*/
